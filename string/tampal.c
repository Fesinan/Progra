#include <stdio.h>
#include <string.h>
#define tam 10

int main (void) {
	char palabra[tam], copia[tam];
	int i;

	for (i = 0; i < 10; i++) {
		printf("Ingrese una Palabra: ");
		gets(palabra);
	}




}
